\RequirePackage{iftex}

\RequirePackage[tbtags]{amsmath}
\RequirePackage[thmtools-compat]{keytheorems}
\RequirePackage{amssymb,diffcoeff,extarrows,etoolbox,mathtools,overarrows,tensor}
\RequirePackage{array,tabularx}

\RequirePackage[backend=biber,style=alphabetic,backref=true]{biblatex}
\DeclareFieldFormat{postnote}{#1}
\DeclareFieldFormat{multipostnote}{#1}

\RequirePackage{bm}
\RequirePackage{booktabs}
\RequirePackage{cancel}
\RequirePackage{csquotes}
\RequirePackage{draftwatermark}
\RequirePackage[shortlabels]{enumitem}
\RequirePackage[lastexercise]{exercise}
\RequirePackage{fancyhdr}
\RequirePackage{fixdif}
\RequirePackage{graphbox,makecell,multicol,setspace}
\RequirePackage{imakeidx}
\RequirePackage[indent]{parskip}
\RequirePackage{pdfpages}
\RequirePackage{scalerel}
\RequirePackage{siunitx}
\RequirePackage[most]{tcolorbox}

% Cannot be dumped by LuaLaTeX
\RequirePackage{adjustbox,trimclip}
\RequirePackage{standalone}
\RequirePackage{tikz-cd}

\RequirePackage[
	pdfstartview=FitH,
	bookmarksnumbered=true,
	bookmarksopen=true,
	colorlinks=true,
	citecolor=darkred,
	linkcolor=darkblue,
	unicode,
]{hyperref}
\RequirePackage{zref-clever}

\zcsetup{hyperref=true, nameinlink=true}
\newcommand{\zcrename}[2]{\zcRefTypeSetup{#1}{
		Name-sg=#2,
		name-sg=#2,
		Name-pl=#2,
		name-pl=#2,
		Name-sg-ab=#2,
		name-sg-ab=#2,
		Name-pl-ab=#2,
		name-pl-ab=#2
	}}

\newcommand{\zcnoname}[1]{\zcrename{#1}{}}

\zcnoname{equation}

\RequirePackage{geometry}

\geometry{left=2cm,right=2cm,top=2cm,bottom=2cm}

\tikzset{elegant/.style={smooth, red, thick, samples=101}}
\tikzcdset{arrow style=math font}

\definecolor{darkred}{rgb}{0.6,0,0}
\definecolor{darkblue}{rgb}{0,0,0.55}

\renewcommand{\fontsubfuzz}{\maxdimen}

\setcounter{secnumdepth}{3}

\raggedbottom

\makeatletter

\newcommand{\pushright}[1]{\ifmeasuring@#1\else\omit\hfill$\displaystyle#1$\fi\ignorespaces}

\newcommand{\lowermathbox}[1]{\mathpalette\lowermathbox@{#1}}
\newcommand{\lowermathbox@}[2]{
	\settoheight{\@tempdima}{\m@th$\smallermathstyle{X}$}
	\raisebox{-0.5\@tempdima}{\m@th$#1#2$}}

\NewOverArrowCommand{\rightptarrow@}{%
	start={},
	middle=\harrowextender,
	end={\clipbox{.5em 0pt 0pt 0pt}{$\rightharpoonup\mathrel{\mspace{-15mu}}\rightharpoonup$}},
	trim start=0,
	trim middle=3,
	trim end=2,
	shift left=5,
	shift right=-1
}

\NewOverArrowCommand{\rightptarrowx@}{%
	start={\hphantom{\smallermathstyle X}},
	middle=\harrowextender,
	end={\clipbox{.5em 0pt 0pt 0pt}{$\rightharpoonup\mathrel{\mspace{-15mu}}\rightharpoonup$}},
	trim start=1,
	trim middle=2,
	trim end=1,
	shift left=-2,
	shift right=-2
}

\NewDocumentCommand{\rightparatrans}{ o m }{
	\IfNoValueTF{#1}
	{ \rightptarrow@{#2} }
	{ \settowidth{\@tempdima}{$#1$}
		\tensor[^{\lowermathbox{\smallermathstyle{#1}}\mspace{-12mu}}]{{\rightptarrowx@{#2}}}{} }
}

\NewOverArrowCommand{\leftptarrow@}{%
	start={\clipbox{0pt 0pt .5em 0pt}{$\leftharpoonup\mathrel{\mspace{-15mu}}\leftharpoonup$}},
	middle=\harrowextender,
	end={},
	trim start=2,
	trim middle=3,
	trim end=0,
	shift left=2,
	shift right=2
}

\NewOverArrowCommand{\leftptarrowx@}{%
	start={\clipbox{0pt 0pt .5em 0pt}{$\leftharpoonup\mathrel{\mspace{-15mu}}\leftharpoonup$}},
	middle=\harrowextender,
	end={\hphantom{\smallermathstyle X}},
	trim start=1,
	trim middle=2,
	trim end=0,
	shift left=2,
	shift right=-4
}

\NewDocumentCommand{\leftparatrans}{ o m }{
	\IfNoValueTF{#1}
	{ \leftptarrow@{#2} }
	{ \settowidth{\@tempdima}{$#1$}
		\tensor{{\leftptarrowx@{#2}}}{^{\mspace{-12mu}\lowermathbox{\smallermathstyle{#1}}}} }
}

\makeatother

\DeclarePairedDelimiter\bra{\lparen}{\rparen}
\DeclarePairedDelimiterX\dbra[2]{\lparen}{\rparen}{#1,#2}
\DeclarePairedDelimiter\Bra{\lbrace}{\rbrace}
\DeclarePairedDelimiterX\set[2]{\lbrace}{\rbrace}{\,#1\mathrel{}\delimsize\vert\mathrel{}#2\,}
\DeclarePairedDelimiter\sqbra{\lbrack}{\rbrack}
\DeclarePairedDelimiter\angbra{\langle}{\rangle}
\DeclarePairedDelimiter\abs{\lvert}{\rvert}
\DeclarePairedDelimiter\norm{\lVert}{\rVert}
\DeclarePairedDelimiter\ceil{\lceil}{\rceil}
\DeclarePairedDelimiter\floor{\lfloor}{\rfloor}

\newcommand{\q}[2]{{\ThisStyle{
			\mathord{
				\kern-\nulldelimiterspace
				\left.
				\raisebox{.25\LMex}{\ensuremath{\SavedStyle{#1}}}
				\kern-\nulldelimiterspace
				\middle\slash
				\kern-\nulldelimiterspace
				\raisebox{-.25\LMex}{\ensuremath{\SavedStyle{#2}}}
				\right.
				\kern-\nulldelimiterspace
			}
		}}}
\newcommand{\coq}[2]{{\ThisStyle{
			\mathord{
				\kern-\nulldelimiterspace
				\left.
				\raisebox{-.25\LMex}{\ensuremath{\SavedStyle{#1}}}
				\middle\backslash
				\raisebox{.25\LMex}{\ensuremath{\SavedStyle{#2}}}
				\right.
				\kern-\nulldelimiterspace
			}
		}}}

\newcommand{\rmnum}[1]{{\romannumeral#1}}
\newcommand{\Rmnum}[1]{{\uppercase\expandafter{\romannumeral#1}}}

\newcommand{\inv}[1]{\frac{1}{#1}}
\newcommand{\toinf}[2][]{\lim_{#2\rightarrow#1\infty}}
\newcommand{\toinfsup}[2][]{\varlimsup_{#2\rightarrow#1\infty}}
\newcommand{\toinfinf}[2][]{\varliminf_{#2\rightarrow#1\infty}}
\newcommand{\series}[1][n=1]{\sum_{#1}^\infty}
\newcommand{\product}[1][n=1]{\prod_{#1}^\infty}

\newcommand{\e}{\mathrm{e}}
\newcommand{\transpose}[1]{{#1}^{\mathsf{T}}}

\newcommand{\infint}[3][x]{\int_{#2}^{+\infty}#3\d #1}
\newcommand{\vepsl}{\varepsilon}

\newcommand{\fserc}[2][n]{\inv{\pi}\int_{-\pi}^\pi#2\cos#1x\dx}
\newcommand{\fsers}[2][n]{\inv{\pi}\int_{-\pi}^\pi#2\sin#1x\dx}
\newcommand{\fsero}[1]{\inv{2\pi}\int_{-\pi}^\pi#1\dx}

\newcommand{\sint}{\int\limits}
\newcommand{\dint}{\iint\limits}
\newcommand{\tint}{\iiint\limits}

\newcommand{\restrict}[3][]{{\left.{#2}\right|_{#3}^{#1}}}
\newcommand{\induce}[3][]{{\left.{#2}\right\uparrow^{#3}_{#1}}}

\newcommand{\sumcyc}{\sum_{\text{cyc}}}

\newcommand{\ol}{\overline}
\newcommand{\ul}{\underline}
\newcommand{\wh}{\widehat}
\newcommand{\wt}{\widetilde}

\newcommand{\id}{\mathrm{id}}

\ExplSyntaxOn

\str_map_inline:nn { x X y Y z Z } {
	\cs_new_protected:cpn { #1 } { \bra { #1 } }
	\cs_new_protected:cpn { d#1 } { \d #1 }
}

\clist_map_inline:nn { p, v, b, B, V } {
	\NewDocumentEnvironment { g #1 mtrx } { }
	{ \setcellgapes{5pt}\makegapedcells\begin{#1matrix} }
	{ \end{#1matrix} }
}

\cs_set_eq:cc { __zz_i_text: } { i }
\RenewDocumentCommand { \i } { } {
	\mode_if_math:TF { \mathrm{i} } { \__zz_i_text: }
}

\cs_set_eq:cc { __zz_j_text: } { j }
\RenewDocumentCommand { \j } { } {
	\mode_if_math:TF { \mathrm{j} } { \__zz_j_text: }
}

\cs_set_eq:cc { __zz_k_text: } { k }
\RenewDocumentCommand { \k } { } {
	\mode_if_math:TF { \mathrm{k} } { \__zz_k_text: }
}

\cs_set_eq:cc { __zz_P_text: } { P }
\RenewDocumentCommand { \P } { } {
	\mode_if_math:TF { \__zz_P_math:n } { \__zz_P_text: }
}

\NewDocumentCommand { \D } { m } { \diff{}{#1} }

\letdif{\laplacian}{Delta}

\newcommand{\del}{\partial}

\bool_if:nTF { \sys_if_engine_xetex_p: || \sys_if_engine_luatex_p: } {
	\newdif*{\dbar}{\bar{\partialnondif}}
} {
	\newdif{\dbar}{\bar{\partialnondif}}
}

% region Define commands from lists
\msg_new:nnn { zz-math } { cmd-already-defined } {
	Command~\c_backslash_str#1~is~already~defined!
}

\cs_new_protected:Npn \__zz_define_cmd:nn #1#2 {
	\seq_set_split:Nnn \l_tmpa_seq { = } { #2 }
	\tl_set:Nx \l_tmpa_tl { \seq_item:Nn \l_tmpa_seq { 1 } }
	\int_compare:nNnTF { \seq_count:N \l_tmpa_seq } > { 1 } {
			\tl_set:Nx \l_tmpb_tl { \seq_item:Nn \l_tmpa_seq { 2 } }
		} {
			\tl_set:Nx \l_tmpb_tl { \l_tmpa_tl }
		}

	\cs_if_exist:cT { \l_tmpa_tl } {
		\msg_fatal:nnx { zz-math } { cmd-already-defined } { \l_tmpa_tl }
	}

	\str_if_eq:nnT { #1 } { mathsf } {
		\iow_log:x { Declaring~mathsf~name:~\exp_not:c { \l_tmpa_tl }~->~{ \exp_not:N \mathsf { \exp_not:V \l_tmpb_tl } } }
		\exp_args:Ncx \newcommand { \l_tmpa_tl } { { \exp_not:N \mathsf { \exp_not:V \l_tmpb_tl } } }
	}
	\str_if_eq:nnT { #1 } { DeclareMathOperator } {
		\iow_log:x { Declaring~operator:~\exp_not:c { \l_tmpa_tl }~->~\exp_not:V \l_tmpb_tl }
		\exp_args:NcV \DeclareMathOperator { \l_tmpa_tl } \l_tmpb_tl
	}
}
% endregion

% region Define categories
\clist_const:Nn \l_zz_mathsf_name_list
{
	Cat,		% categories
	MonCat,		% monoidal categories
	Fun,		% functors
	Fib,		% fibrations
	Pair,		% pairs
	Set,		% sets
	Top,		% topological spaces
	Op,			% open sets
	Ofd,		% orbifolds
	Mfd,		% manifolds
	FBun,		% fibre bundles
	VBun,		% vector bundles
	PBun,		% principal bundles
	OrbC=Orb,	% orbit category
	Mon,		% monoids
	Grp,		% groups
	Ab,			% abelian groups
	TopGrp,		% topological groups
	LieGrp,		% Lie groups
	LieGrpd,	% Lie groupoids
	LieAlg,		% Lie algebras
	Grpd,		% groupoids
	Ring,		% rings
	CRing,		% commutative rings
	Mod,		% modules
	Alg,		% algebras
	CAlg,		% commutative algebras
	Field,		% fields
	Vect,		% vector spaces
	AlgVar,		% algebraic varieties
	PSh,		% presheaves
	Sh,			% sheaves
	Aff,		% affine schemes
	Sch,		% schemes
	GrCRing,	% graded commutative rings
	GrMod,		% graded modules
	Cone,		% cones
	cCone,		% cocones
	Wdg,		% wedges
	cWdg,		% cowedges
	El,			% category of elements
}

\clist_map_inline:Nn \l_zz_mathsf_name_list {
	\__zz_define_cmd:nn { mathsf } { #1 }
}

\DeclarePairedDelimiterX\commacat[2]{\lparen}{\rparen}{
	#1 \mathrel{}\delimsize\downarrow\mathrel{} #2
} % comma category

\cs_set_eq:cc { __zz_Fun: } { Fun }
\RenewDocumentCommand { \Fun } { s m m } {
	\IfBooleanTF { #1 } { \__zz_Fun: } { \__zz_Fun: \dbra*{#2}{#3} }
}

\cs_set_eq:cc { __zz_Alg: } { Alg }
\RenewDocumentCommand { \Alg } { s m } {
    \IfBooleanTF { #1 } { \__zz_Alg: } { #2 \text{-} \__zz_Alg: }
}

\cs_set_eq:cc { __zz_CAlg: } { CAlg }
\RenewDocumentCommand { \CAlg } { s m } {
    \IfBooleanTF { #1 } { \__zz_CAlg: } { #2 \text{-} \__zz_CAlg: }
}

\newcommand{\LMod}[1]{#1\text{-}\Mod} % left modules
\newcommand{\RMod}[1]{\Mod\text{-}#1} % right modules
% endregion

% region Define math operators
\clist_const:Nn \l_zz_math_operator_list
{
	yo=\text{よ},				% Yoneda embedding
	oy=\text{\reflectbox{よ}},	% Yoneda embedding
	EAut=\mathsf{Aut},			% enriched automorphism
	EIso=\mathsf{Iso},			% enriched isomorphism
	EEnd=\mathsf{End},			% enriched endomorphism
	EHom=\mathsf{Hom},			% enriched homomorphism
	% EAut=\mcalA ut,				% enriched automorphism
	% EIso=\mcalI so,				% enriched isomorphism
	% EEnd=\mcalE nd,				% enriched endomorphism
	% EHom=\mcalH om,				% enriched homomorphism
	Pf,							% Pfaffian
	Log,						% principal branch of logarithm
	Def,						% definable sets
	ev,							% evaluation
	im,							% image
	codim,						% codimension
	itr=int,					% interior
	tr,							% trace
	ch=char,					% characteristic
	cond,						% conductor
	sgn,						% sign
	sing,						% singular
	diam,						% diameter
	diag,						% diagonal operator
	arcsinh,					% inverse hyperbolic sine
	arccosh,					% inverse hyperbolic cosine
	arctanh,					% inverse hyperbolic tangent
	arccoth,					% inverse hyperbolic cotangent
	pr,							% projection
	proj,						% projection
	rank,						% rank
	supp,						% support
	grad=\mathbf{grad},			% gradient
	Div,						% divisor
	PDiv,						% principal divisor
	div,						% divergence
	curl,						% curl
	rot=\mathbf{rot},			% rotation
	Ric=Ricci,					% Ricci curvature
	Ein=Einstein,				% Einstein tensor
	len,						% length
	inc,						% inclusion
	ind,						% index
	rad,						% radical
	soc,						% socle
	ann,						% annihilator set
	Ann,						% annihilator
	Der,						% derivation
	Aut,						% automorphism
	Iso,						% isomorphism
	End,						% endomorphism
	Hom,						% homomorphism
	Nat,						% natural transformation
	Lie,						% Lie functor
	Syl,						% Sylow subgroup
	Orb,						% orbit
	orb,						% orbit set
	Stab,						% stabilizer
	Gal,						% Galois group
	M,							% matrix
	GL,							% general linear group
	gl=\mathfrak{gl},			% general linear Lie algebra
	SL,							% special linear group
	sl=\mathfrak{sl},			% special linear Lie algebra
	U,							% unitary group
	u=\mathfrak{u},				% unitary Lie algebra
	SU,							% special unitary group
	su=\mathfrak{su},			% special unitary Lie algebra
	O,							% orthogonal group
	o=\mathfrak{o},				% orthogonal Lie algebra
	SO,							% special orthogonal group
	so=\mathfrak{so},			% special orthogonal Lie algebra
	hol,						% linear holonomy group
	Hol,						% affine holonomy group
	ord,						% order
	Norm,						% norm
	Trace,						% trace
	A,
	con,
	ad,
	Ad,
	spanspace=span,				% span
	gr,							% graded
	Eff,						% effective
	DscDt=DD,					% descent datum
	dis,						% discrete category
	Spec,						% spectrum
	Specm,						% maximal spectrum
	Frac,						% fraction field
	hght=ht,					% height
	depth,						% depth
	T,
	Proj,						% projective spectrum
	vol,						% volume
	covol,						% covolume
	Gr,							% Grassmannian
	res,						% residue
	Ext,						% extension
	Tor,						% torsion
	Lan,						% left Kan extension
	Ran,						% right Kan extension
	ob,							% object
	mor,						% morphism
	dom,						% domain
	codom,						% codomain
	coker,						% cokernel
	cov,						% covering
	Cov,						% covering
	Sv,							% sieve
	rt,							% restriction
	conv,						% convex hull
	Jac,						% Jacobian
	Ell,						% elliptic curve
	Pic,						% Picard group
	Cl,							% class group
	CCl={\BbbC l},				% complexified Clifford algebra
	Pin,						% Pin group
	Pinc={Pin^c},				% Pin^c group
	pin=\mathfrak{pin},			% Pin Lie algebra
	Spin,						% Spin group
	Spinc={Spin^c},			% Spin^c group
	spin=\mathfrak{spin},		% Spin Lie algebra
}

\cs_undefine:N \div

\clist_const:Nn \l_zz_dual_mode_cmd_list
{
	sl,
	u,
	O,
	o,
}

\clist_map_inline:Nn \l_zz_dual_mode_cmd_list
{
	\cs_set_eq:cc { __zz_#1_text: } { #1 }
	\cs_undefine:c { #1 }
}

\clist_map_inline:Nn \l_zz_math_operator_list {
	\__zz_define_cmd:nn { DeclareMathOperator } { #1 }
}

\clist_map_inline:Nn \l_zz_dual_mode_cmd_list
{
	\cs_set_eq:cc { __zz_#1_math: } { #1 }
	\cs_undefine:c { #1 }
}

\NewDocumentCommand { \O } { } {
	\mode_if_math:TF { \__zz_O_math: } { \__zz_O_text: }
}

\NewDocumentCommand { \o } { } {
	\mode_if_math:TF { \__zz_o_math: } { \__zz_o_text: }
}

\cs_new_protected:Npn \u {
	\mode_if_math:TF { \__zz_u_math: } { \__zz_u_text: }
}
% endregion

% region Define letters
\cs_new_protected:Npn \__zz_define_letters:nn #1#2 {
	\cs_set_protected:Npn \__zz_tmp_loop:n ##1 {
		\tl_set:Nx \l_tmpa_tl { \char_generate:nn { ##1 } { 11 } }
		\tl_set:Nx \l_tmpb_tl { #1 \l_tmpa_tl }

		\cs_if_exist:cTF { \l_tmpb_tl } {
			\msg_warning:nnx { zz-math } { cmd-already-defined } { \l_tmpb_tl }
		} {
			\exp_args:Ncx \newcommand { \l_tmpb_tl } { { \exp_not:n { #2 } { \l_tmpa_tl } } }
		}
	}

	\int_step_function:nnN { `A } { `Z } \__zz_tmp_loop:n
	\int_step_function:nnN { `a } { `z } \__zz_tmp_loop:n
}

\__zz_define_letters:nn { mcal } { \mathcal }
\sys_if_engine_pdftex:T {
	\RequirePackage{amsfonts}
	\RequirePackage{mathrsfs}

	\cs_if_exist:NF \mscr {
		\cs_new_protected:Npn \mscr #1 { \mathscr { #1 } }
	}
	\__zz_define_letters:nn { mscr } { \mathscr }

	\cs_if_exist:NF \Bbb {
		\cs_new_protected:Npn \Bbb #1 { \mathbb { #1 } }
	}
	\__zz_define_letters:nn { Bbb } { \mathbb }

	\cs_if_exist:NF \mfrak {
		\cs_new_protected:Npn \mfrak #1 { \mathfrak { #1 } }
	}
	\__zz_define_letters:nn { mfrak } { \mathfrak }
}
% endregion

\ExplSyntaxOff
